[package]
name = "portfolio-serve"
version = "0.3.0"
edition = "2024"

[dependencies]
axum = "0.8.*"
tokio = { version = "1.*.*", features = ["full"] }
tower-http = { version = "0.7.*", features = ["fs", "compression-full"] }